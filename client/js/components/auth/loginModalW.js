
/*
  let wrapper = document.querySelector('.wrapper');

  let windowBackground = document.createElement('div');
  windowBackground.className = 'modal-todo-background';
  
  let windowForm = document.createElement('form');

  let user = document.createElement('input');
  user.setAttribute('type', 'text');
  user.setAttribute('id', 'input-user');
  user.setAttribute('placeholder', 'user');

  let password = document.createElement('input');
  password.setAttribute('type', 'password');
  password.setAttribute('id', 'input-password');
  password.setAttribute('placeholder', 'password');

  windowForm.append(user);
  windowForm.append(password);
  windowBackground.append(windowForm);
  wrapper.append(windowBackground); 

*/ 

